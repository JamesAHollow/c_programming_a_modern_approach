#include <stdio.h>

int main()
{
   int i;
   float x, y;

   printf("\nEnter the pattern in the exercise: ");
   scanf("%f%d%f", &x, &i, &y);

   printf("\nx = %f\n", x);
   printf("i = %d\n", i);
   printf("y = %f\n", y);

   return 0;
}
